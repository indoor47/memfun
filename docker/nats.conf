# NATS Server configuration for Memfun
# -------------------------------------------------------

# JetStream — persistent messaging
jetstream {
    max_mem: 1GB
    max_file: 10GB
    store_dir: /data/jetstream
}

# HTTP monitoring
http_port: 8222
